[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: isolated behavior with no external service dependency
    integration: behavior across local filesystem, database, or module boundaries
    api: JSON protocol behavior
    e2e: complete user-visible system flow
    security: authentication, authorization, or sensitive-data behavior
